# Frontend environment variables
# Base URL of the backend API (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Hardcoded worker ID to display on the dashboard
NEXT_PUBLIC_WORKER_ID=worker-123
