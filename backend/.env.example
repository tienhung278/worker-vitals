# Server
PORT=3000

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres

# App defaults
DEFAULT_WORKER_ID=worker-123

# CORS
# Comma-separated list of allowed origins. Leave empty to allow any origin in dev.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
